# design-system-diametral

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `diamorval/design-system-diametral`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, with `ready-for-agent` mapped to the existing `Sandcastle` label. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
