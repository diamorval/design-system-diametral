import { Wordmark } from "@diametral/ui/components/wordmark"

// The lockup is `currentColor`, so it recolours with the surrounding text —
// no separate light/dark SVG to swap.
export default function WordmarkBasic() {
  return <Wordmark />
}
