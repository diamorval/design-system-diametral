import { Wordmark } from "@diametral/ui/components/wordmark"

// The wordmark set inside the symbol — avatars and app-icon-style
// placements, where the horizontal lockup would be too wide.
export default function WordmarkSquare() {
  return <Wordmark variant="square" />
}
